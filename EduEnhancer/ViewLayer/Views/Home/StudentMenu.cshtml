@model DomainModel.Student

@{
    ViewData["Title"] = "Student";
}

<div id="header">
    <div id="header-title">@ViewData["Title"]</div>
    <div id="header-notice">@ViewBag.Message</div>
</div>

<div id="main">
    <div class="panel panel-medium">
        <div class="panel-content">
            <a asp-action="StudentAvailableTestList" class="button">Vyřešit test</a>
        </div>
        <div class="panel-content">
            <a asp-action="BrowseSolvedTestList" class="button">Prohlížení vyřešených testů</a>
        </div>
    </div>

    <div class="panel panel-medium">
        <table id="user-information" class="panel-table">
            <tr>
                <th>Jméno</th>
                <td>@Model.FullName()</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>@Model.Login</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>@Model.Email</td>
            </tr>
        </table>
    </div>
</div>

<a id="backBtn" class="button" asp-action="Index">Zpět</a>

<a id="signOutBtn" class="button" asp-controller="Account" asp-action="GoogleSignOut">Odhlásit se</a>
