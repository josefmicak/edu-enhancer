@model IEnumerable<DomainModel.TestTemplate>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TestNameIdentifier)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TestNameIdentifier)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TestNumberIdentifier">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TestNumberIdentifier">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TestNumberIdentifier">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
