@model ViewLayer.Models.StudentMenuModel

@using DomainModel;

@{
    ViewData["Title"] = @Model.Title;
}

<div class="text-center">
    <h1>@Model.Title</h1>

    <div id="student-list">
        <table>
            <tr>
                <th>Jméno</th>
                <th>Login</th>
                <th>Identifikátor</th>
                <th>Zvolit</th>
            </tr>
            @{
                int i = 0;
                foreach (Student student in @Model.Students)
                {
                    <tr>
                        <td>@student.FirstName @student.LastName</td>
                        <td>@student.Login</td>
                        <td>@student.StudentIdentifier</td>
                        <td><a class="button" id="selectStudentBtn_@i" title="Zvolit studenta" asp-area="" asp-controller="Home" asp-action="BrowseSolvedTestList"
                        asp-route-studentIdentifier="@student.StudentIdentifier">Zvolit studenta</a></td> 
                    </tr>
                    i++;
                }
            }
        </table>
    </div>

</div>

<div id="backBtn"><a asp-action="Index">Zpět</a></div>

<div id="signOutBtn"><a asp-controller="Account" asp-action="GoogleSignOut">Odhlásit se</a></div>